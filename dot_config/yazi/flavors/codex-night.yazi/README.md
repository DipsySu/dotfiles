# codex-night.yazi

个人 flavor，配色与 Windows Terminal 的 "Codex Night" 方案一致。

- `flavor.toml`：UI 配色（Codex Night 调色板）
- `tmtheme.xml`：代码预览高亮，借用 Tokyo Night 的 tmTheme（见 LICENSE-tmtheme）

在 `~/.config/yazi/theme.toml` 中启用：

```toml
[flavor]
dark  = "codex-night"
light = "codex-night"
```
